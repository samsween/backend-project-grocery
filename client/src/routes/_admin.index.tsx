
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/_admin/")({
  component: () => (
    <div className="px-10 pt-10 flex gap-2 w-full">TODO Make dashboard</div>
  ),
});
